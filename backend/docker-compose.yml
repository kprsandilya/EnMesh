services:
  enmesh-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENMESH_DEVICE=cpu
      - ENMESH_RESOLUTION=128
      - ENMESH_CHUNK_SIZE=4096
    volumes:
      - model-cache:/root/.cache
    restart: unless-stopped

volumes:
  model-cache:
