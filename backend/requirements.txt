# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18
pydantic>=2.9.0

# Image / mesh I/O (mesh endpoint uses a placeholder unless TripoSR is on PYTHONPATH)
Pillow>=11.0.0
trimesh>=4.5.0
numpy>=1.26.0

# Placement classification (downloads model on first use)
sentence-transformers>=3.0.0
