# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18
pydantic>=2.9.0

# Mesh I/O
Pillow>=11.0.0
trimesh>=4.5.0
numpy>=1.26.0

# Auto Layout (sentence-transformers pulls torch)
sentence-transformers>=3.0.0

# Optional: TripoSR background removal when `tsr` is importable
rembg>=2.0.57
